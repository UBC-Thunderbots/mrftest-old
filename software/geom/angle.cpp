#include "geom/angle.h"

const Angle Angle::ZERO;

const Angle Angle::QUARTER(M_PI / 2.0);

const Angle Angle::HALF(M_PI);

const Angle Angle::THREE_QUARTER(3.0 / 2.0 * M_PI);

const Angle Angle::FULL(2.0 * M_PI);

