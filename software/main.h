#ifndef MAIN_H
#define MAIN_H

/**
 * \brief The application’s entry point, with exceptions handled usefully.
 *
 * \param[in] argc the number of command-line parameters, plus one
 *
 * \param[in] argv the command-line parameters, including the executable name
 */
int app_main(int argc, char **argv);

#endif
