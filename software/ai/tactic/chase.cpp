#include "ai/tactic/chase.h"

//chase::chase(ball::ptr ball, field::ptr field, controlled_team::ptr team, player::ptr player) : tactic(ball, field, team, player) {
//	move::ptr move_tactic(new move(ball,field,team,player));
//	move_tactic->set_position(ball->position());
//	sub_tactics.push_back(move_tactic);
//}

