# minimum CMake version
# using version 3.9 as it was bundled with CLion at the time that this CMake file was created
cmake_minimum_required(VERSION 3.9)

# project name
project(thunderbots)

add_subdirectory(software)